## Start the app

```shell
docker compose up -d
```

Interface will be available via 8023 port in your browser:

http://localhost:8023

## Clean up

Clean up everything created in system:

```shell
docker compose down
```
